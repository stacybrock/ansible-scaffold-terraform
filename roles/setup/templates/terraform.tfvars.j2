terragrunt = {
  remote_state {
    backend = "s3"
    config {
      bucket         = "{{ state_bucket_name }}"
      profile        = "{{ config_name }}"
      key            = "${path_relative_to_include()}/terraform.tfstate"
      region         = "{{ terragrunt_state.aws_region }}"
      encrypt        = true
      dynamodb_table = "{{ dynamodb_table_name }}"
    }
  }
}
